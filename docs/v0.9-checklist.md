# ClarityClaw v0.9 Checklist

Concrete implementation checklist for `v0.9` production hardening.

Status: complete. The next planning surface is [docs/v1.0-checklist.md](/home/cpgrant/development/codex/clarityos/docs/v1.0-checklist.md).

## Slice 1: State Versioning And Migrations

- [x] `runtime/state.py`
  Add shared persisted-state envelope helpers with `{schema, version, payload}` and backward-compatible unwrapping for legacy raw JSON files.
- [x] `runtime/state.py`
  Reject invalid persisted-state version formats, schema mismatches, and forward versions newer than the current runtime supports.
- [x] `runtime/workflow.py`
  Write versioned workflow files and continue loading legacy unversioned workflow snapshots.
- [x] `runtime/queue.py`
  Write versioned job files and continue loading legacy unversioned job snapshots.
- [x] `runtime/memory.py`
  Write versioned memory files and continue loading legacy unversioned memory snapshots.
- [x] `runtime/worker.py`
  Write versioned worker files and continue loading legacy unversioned worker snapshots.
- [x] `runtime/approval.py`
  Write versioned approval files and continue loading legacy unversioned approval snapshots.
- [x] `runtime/artifact.py`
  Write versioned artifact files and continue loading legacy unversioned artifact snapshots.
- [x] `api/main.py`
  Add operator-visible persisted-state schema catalog and per-record inspection endpoints.
- [x] `tests/test_workflow.py`
  Expand workflow migration coverage into missing legacy fields, invalid schema, and forward-version handling.
- [x] `tests/test_queue.py`
  Expand queue migration coverage into mixed legacy/versioned listing and legacy rewrite during failure/update flows.
- [x] `tests/test_memory.py`
  Add list/query migration coverage across mixed legacy and versioned memory files and verify legacy rewrite on update.
- [x] `tests/test_worker.py`
  Add mixed legacy/versioned worker coverage and verify legacy rewrite on update.
- [x] `tests/test_persistence_versions.py`
  Cover versioned writes, legacy reads, invalid schema, and forward-version rejection for persisted state.
- [x] `api/main.py`
  Add operator migration endpoint for in-place rewrite of legacy persisted state records.
- [x] `api/main.py`
  Add bulk operator migration endpoints for per-kind and full-runtime state migration.

## Slice 2: Operator Recovery And Pruning Controls

- [x] `runtime/control_plane.py`
  Add recovery summaries for related jobs/workers and first safe workflow-scoped recovery actions.
- [x] `runtime/queue.py`
  Add explicit prune/recover helpers for dead-letter and stale queued state.
- [x] `runtime/worker.py`
  Add orphaned-lease repair and operator-safe worker reset flows.
- [x] `runtime/workflow_runner.py`
  Add replay and safe resume entry points for recoverable workflow states.
- [x] `api/main.py`
  Expose operator recovery, queue cleanup, worker repair/reset, and workflow replay/resume-safe endpoints.

## Slice 3: Observability And Incident Debugging

- [x] `runtime/trace.py`
  Add stable correlation IDs across workflows, jobs, workers, approvals, memories, and artifacts.
- [x] `runtime/trace.py`
  Add read-side trace summaries with failure classification for incident correlation.
- [x] `runtime/control_plane.py`
  Surface stable correlation IDs in workflow and incident views, including delegated/child workflow trace linkage.
- [x] `runtime/workflow.py` / `runtime/queue.py` / `runtime/worker.py`
  Persist structured transition histories so workflow, job, and worker timelines survive restarts and operator inspection.
- [x] `runtime/control_plane.py`
  Surface richer incident summaries, transition history, failure classification, and unified causality chains.
- [x] `runtime/control_plane.py`
  Add first transition-timeline views for workflow incidents and control-plane debugging.
- [x] `runtime/control_plane.py`
  Add first incident classification counts and recent event summaries across workflows, jobs, workers, and traces.
- [x] `runtime/queue.py`
  Add queue health metrics such as age, retry backlog, claim age, and lifecycle history views.
- [x] `runtime/queue.py`
  Add first queue trend summaries for retries, dead letters, reclaimed jobs, and recent queue events.
- [x] `runtime/worker.py`
  Add worker health/debug summaries for lease churn, lifecycle history, reclaim context, and active dispatch state.
- [x] `runtime/worker.py`
  Add first worker trend summaries for heartbeat age, orphaned workers, expired leases, and recent worker issues.
- [x] `api/main.py`
  Add incident-friendly summary endpoints that correlate control-plane and trace state.
- [x] `api/main.py`
  Expose first queue health, worker health, and workflow incident correlation endpoints.

## Slice 4: Auth, Deployment, And Resilience Hardening

- [x] `api/main.py`
  Protect operator endpoints with explicit auth and operator-only guards.
- [x] `runtime/policy.py`
  Tighten policy defaults for production deployment and operator actions.
- [x] `README.md`
  Document deployment assumptions, retention expectations, and operator auth setup.
- [x] `requirements.txt`
  Add any explicit runtime dependencies needed for production auth/observability once chosen.
- [x] `tests/`
  Add restart, partial-failure, and recovery validation that exercises the hardened runtime end to end.
