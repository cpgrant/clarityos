# ClarityOS v0.8 Snapshot

Milestone snapshot for `v0.8`, the multi-agent coordination release.

## Status

- Release: `v0.8`
- Focus after this release: `v0.9` production hardening
- Next target after this release: `v0.9` slice 1 state versioning and migrations

## What v0.8 Added

- bounded child workflow delegation
- explicit child lineage through parent/root workflow metadata
- delegation contracts with child role assignment and allowed capability bounds
- scoped shared-memory handoff by selected `memory_id`
- operator-facing child failure inspection and containment reporting

## Acceptance Criteria Met

- Workflows can coordinate bounded child agents with explicit lineage and role assignment.
- Agent delegation respects configurable limits for depth, fan-out, and allowed capabilities.
- Agents can read shared memory selectively and write scoped memory intentionally.
- Multi-agent execution is inspectable through traces, workflow state, and control-plane views.
- Failures in one child workflow do not silently corrupt sibling or parent workflow state.
- Automated tests cover delegation, lineage, scoped memory access, and failure isolation.

## Key Runtime Notes

- The workflow control plane now shows child workflow summaries, shared memory handoff, failed child details, and containment state.
- Trace payload versioning moved forward with the `v0.8` release.
- The canonical up-to-date documentation remains the repository root `README.md`.

## Validation

Automated verification for this milestone used:

```bash
python -m unittest discover -s tests -v
```
