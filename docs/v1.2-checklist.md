# ClarityClaw v1.2 Checklist

Concrete implementation checklist for `v1.2`, the assistant deployment hardening milestone.

Status: complete

## Slice 1: Session Ownership And Surface Auth

- [x] `api/main.py`
  Add explicit auth and ownership boundaries for assistant-facing and operator-facing surfaces so deployed sessions are not implicitly shared.
- [x] `runtime/session.py`
  Extend session metadata only as needed to track surface ownership and deployment-facing identity without weakening the current explicit model.
- [x] `README.md`
  Document the intended auth/session posture for deployed assistant surfaces.

## Slice 2: Embed Policy And Branded Deployment Profile

- [x] `api/main.py`, `ui/widget.html`, `ui/widget.js`
  Harden widget posture around allowed origins, branding defaults, and deployment-safe embed behavior.
- [x] `docs/roadmap.md`
  Record the narrow supported embed/deployment profile and what remains out of scope.
- [x] `README.md`
  Document embed configuration, origin controls, and branding defaults for operators.

## Slice 3: Assistant Operator Playbooks And Maintenance Flows

- [x] `docs/playbooks/`
  Add playbooks that cover assistant-surface incidents, widget deployment mistakes, session cleanup, and support-oriented operator recovery.
- [x] `runtime/control_plane.py`
  Add any missing compact summaries only if assistant-surface playbooks require them.
- [x] `README.md`
  Link the assistant-surface playbooks from the main docs.

## Slice 4: First Assistant Deployment Path And Release Criteria

- [x] `docs/roadmap.md`
  Define the first narrow deployed assistant use case and the measurable release gates for it.
- [x] `README.md`
  Summarize the supported deployment target and what is still intentionally unsupported after `v1.2`.
- [x] `docs/history/`
  Keep the `v1.1` snapshot and the milestone trail aligned once `v1.2` scope stabilizes.
