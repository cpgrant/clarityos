models:
  # OpenAI-backed models
  fast:
    provider: openai
    provider_id: gpt-4o-mini

  smart:
    provider: openai
    provider_id: gpt-4o

  # Ollama-backed models
  local_fast:
    provider: ollama
    provider_id: llama3.1:latest
